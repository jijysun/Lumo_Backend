-- ─────────────────────────────────────────────────────────────
-- V2 — member.email 유니크 제약 추가 (M-13 / A-2)
--
-- 왜 필요한가
--   ① findByEmail 은 인증된 모든 요청·로그인·가입·중복확인이 타는 최고 빈도 쿼리인데
--      인덱스가 없어 전부 풀스캔이었다.
--   ② DB 제약이 없어 MemberService.signIn 의 "조회 후 저장" 사이에서
--      같은 이메일로 두 행이 생길 수 있었다.
--   유니크 제약은 유니크 인덱스를 만들므로 ①②가 한 번에 해결된다.
--
-- ⚠️ 중복 행이 남아 있으면 ALTER 가 실패한다. 아래 검사 쿼리로 먼저 확인할 것.
--      SELECT email, COUNT(*) c FROM member GROUP BY email HAVING c > 1;
--    중복이 있으면 어느 행을 남길지는 서비스 판단이 필요하므로
--    자동 삭제하지 않는다 — 수동 정리 후 재실행한다.
-- ─────────────────────────────────────────────────────────────

ALTER TABLE `member`
    ADD CONSTRAINT `uk_member_email` UNIQUE (`email`);
